<div id="<%= dom_id post %>">
  <h2><%= post.title %></h2>
  <h4>posted by <%= link_to post.user.full_name, user_path(post.user) %></h4>
  <h6><%= pluralize(post.views, "view") %></h6>
  <hr />
  <div>
    <%= post.body %>
  </div>
</div>
